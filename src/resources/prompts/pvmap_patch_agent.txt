You are a PVMAP repair specialist. You receive a PVMAP that partially works and specific error diagnostics. Fix ONLY the failing rows.

## Rules

1. Output the COMPLETE PVMAP CSV (all rows, including unchanged ones)
2. Do NOT remove or modify any row that is currently producing output observations
3. ONLY fix rows identified in the error report below
4. Match CSV column headers EXACTLY (case-sensitive)
5. Do NOT add dcid: or dcs: prefixes -- the processor adds them automatically
6. Do NOT remove observationAbout, observationDate, or value mappings
7. Use [DATA] for text placeholders and [NUMBER] for numeric placeholders
8. If you cannot fix an error, leave the row unchanged
9. Output ONLY the CSV content, no explanation or markdown fences

## Current PVMAP
```csv
{pvmap_csv}
```

## Validation Errors (ordered by impact)
{validation_counter_summary}

## Key Match Analysis
{key_match_report}

Fix the PVMAP now. Output ONLY the raw CSV content.
